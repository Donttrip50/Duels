-- Generated using PenguEnv (Pro) https://discord.gg/V3PC8CZGZ5

-- generated using PenguEnv v0.6.4 (S)
-- https://discord.gg/V3PC8CZGZ5

local env = getgenv()

local Players = game:GetService("Players")
local fenv = getfenv()
local CoreGui = game:GetService("CoreGui")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")

local BK_Lagger_Replica = CoreGui:FindFirstChild("BK_Lagger_Replica")
CoreGui_BK_Lagger_Replica:Destroy()
local neygga = CoreGui:FindFirstChild("neygga")
CoreGui_neygga:Destroy()
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Parent = CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
local Frame = Instance.new("Frame")
Frame.Position = UDim2.new(0.5, -240, 0.5, -285)
Frame.Parent = ScreenGui
Frame.BackgroundTransparency = 1
Frame.Size = UDim2.new(0, 480, 0, 580)
local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 12)
local UIStroke = Instance.new("UIStroke")
UIStroke.Color = Color3.fromRGB(0, 255, 100)
UIStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
UIStroke.Thickness = 3
local Frame_2 = Instance.new("Frame")
Frame_2.Size = UDim2.new(1, 0, 0, 50)
Frame_2.Position = UDim2.new(0, 0, 0, 0)
Frame_2.Parent = Frame
Frame_2.BackgroundTransparency = 0.25
Frame_2.BorderSizePixel = 0
Frame_2.BackgroundColor3 = Color3.fromRGB(15, 25, 15)
UICorner.Parent = Frame_2
UIStroke.Parent = Frame_2
local UICorner_2 = Instance.new("UICorner")
UICorner_2.CornerRadius = UDim.new(0, 12)
local Frame_3 = Instance.new("Frame")
Frame_3.Size = UDim2.new(1, 0, 1, 0)
Frame_3.Parent = Frame_2
Frame_3.ZIndex = 0
Frame_3.BackgroundTransparency = 0
Frame_3.BackgroundColor3 = Color3.fromRGB(10, 20, 10)
UICorner_2.Parent = Frame_3
local Frame_4 = Instance.new("Frame")
Frame_4.Parent = Frame_3
Frame_4.ZIndex = 1
Frame_4.BackgroundTransparency = 1
Frame_4.Size = UDim2.new(1, 0, 1, 0)
local Frame_5 = Instance.new("Frame")
Frame_5.Size = UDim2.new(0, 2, 1, 0)
Frame_5.BackgroundTransparency = 0.7
Frame_5.Position = UDim2.new(0, 0, 0, 0)
Frame_5.Parent = Frame_4
Frame_5.ZIndex = 1
Frame_5.BorderSizePixel = 0
Frame_5.BackgroundColor3 = Color3.fromRGB(0, 80, 30)
local Frame_6 = Instance.new("Frame")
Frame_6.Size = UDim2.new(0, 2, 1, 0)
Frame_6.BackgroundTransparency = 0.7
Frame_6.Position = UDim2.new(0.1, 0, 0, 0)
Frame_6.Parent = Frame_4
Frame_6.ZIndex = 1
Frame_6.BorderSizePixel = 0
Frame_6.BackgroundColor3 = Color3.fromRGB(0, 80, 30)
local Frame_7 = Instance.new("Frame")
Frame_7.Size = UDim2.new(0, 2, 1, 0)
Frame_7.BackgroundTransparency = 0.7
Frame_7.Position = UDim2.new(0.2, 0, 0, 0)
Frame_7.Parent = Frame_4
Frame_7.ZIndex = 1
Frame_7.BorderSizePixel = 0
Frame_7.BackgroundColor3 = Color3.fromRGB(0, 80, 30)
local Frame_8 = Instance.new("Frame")
Frame_8.Size = UDim2.new(0, 2, 1, 0)
Frame_8.BackgroundTransparency = 0.7
Frame_8.Position = UDim2.new(0.3, 0, 0, 0)
Frame_8.Parent = Frame_4
Frame_8.ZIndex = 1
Frame_8.BorderSizePixel = 0
Frame_8.BackgroundColor3 = Color3.fromRGB(0, 80, 30)
local Frame_9 = Instance.new("Frame")
Frame_9.Size = UDim2.new(0, 2, 1, 0)
Frame_9.BackgroundTransparency = 0.7
Frame_9.Position = UDim2.new(0.4, 0, 0, 0)
Frame_9.Parent = Frame_4
Frame_9.ZIndex = 1
Frame_9.BorderSizePixel = 0
Frame_9.BackgroundColor3 = Color3.fromRGB(0, 80, 30)
local Frame_10 = Instance.new("Frame")
Frame_10.Size = UDim2.new(0, 2, 1, 0)
Frame_10.BackgroundTransparency = 0.7
Frame_10.Position = UDim2.new(0.5, 0, 0, 0)
Frame_10.Parent = Frame_4
Frame_10.ZIndex = 1
Frame_10.BorderSizePixel = 0
Frame_10.BackgroundColor3 = Color3.fromRGB(0, 80, 30)
local Frame_11 = Instance.new("Frame")
Frame_11.Size = UDim2.new(0, 2, 1, 0)
Frame_11.BackgroundTransparency = 0.7
Frame_11.Position = UDim2.new(0.6, 0, 0, 0)
Frame_11.Parent = Frame_4
Frame_11.ZIndex = 1
Frame_11.BorderSizePixel = 0
Frame_11.BackgroundColor3 = Color3.fromRGB(0, 80, 30)
local Frame_12 = Instance.new("Frame")
Frame_12.Size = UDim2.new(0, 2, 1, 0)
Frame_12.BackgroundTransparency = 0.7
Frame_12.Position = UDim2.new(0.7, 0, 0, 0)
Frame_12.Parent = Frame_4
Frame_12.ZIndex = 1
Frame_12.BorderSizePixel = 0
Frame_12.BackgroundColor3 = Color3.fromRGB(0, 80, 30)
local Frame_13 = Instance.new("Frame")
Frame_13.Size = UDim2.new(0, 2, 1, 0)
Frame_13.BackgroundTransparency = 0.7
Frame_13.Position = UDim2.new(0.8, 0, 0, 0)
Frame_13.Parent = Frame_4
Frame_13.ZIndex = 1
Frame_13.BorderSizePixel = 0
Frame_13.BackgroundColor3 = Color3.fromRGB(0, 80, 30)
local Frame_14 = Instance.new("Frame")
Frame_14.Size = UDim2.new(0, 2, 1, 0)
Frame_14.BackgroundTransparency = 0.7
Frame_14.Position = UDim2.new(0.9, 0, 0, 0)
Frame_14.Parent = Frame_4
Frame_14.ZIndex = 1
Frame_14.BorderSizePixel = 0
Frame_14.BackgroundColor3 = Color3.fromRGB(0, 80, 30)
local Frame_15 = Instance.new("Frame")
Frame_15.Size = UDim2.new(0, 2, 1, 0)
Frame_15.BackgroundTransparency = 0.7
Frame_15.Position = UDim2.new(1, 0, 0, 0)
Frame_15.Parent = Frame_4
Frame_15.ZIndex = 1
Frame_15.BorderSizePixel = 0
Frame_15.BackgroundColor3 = Color3.fromRGB(0, 80, 30)
local Frame_16 = Instance.new("Frame")
Frame_16.Size = UDim2.new(1, 0, 0, 2)
Frame_16.BackgroundTransparency = 0.7
Frame_16.Position = UDim2.new(0, 0, 0, 0)
Frame_16.Parent = Frame_4
Frame_16.ZIndex = 1
Frame_16.BorderSizePixel = 0
Frame_16.BackgroundColor3 = Color3.fromRGB(0, 80, 30)
local Frame_17 = Instance.new("Frame")
Frame_17.Size = UDim2.new(1, 0, 0, 2)
Frame_17.BackgroundTransparency = 0.7
Frame_17.Position = UDim2.new(0, 0, 0.1, 0)
Frame_17.Parent = Frame_4
Frame_17.ZIndex = 1
Frame_17.BorderSizePixel = 0
Frame_17.BackgroundColor3 = Color3.fromRGB(0, 80, 30)
local Frame_18 = Instance.new("Frame")
Frame_18.Size = UDim2.new(1, 0, 0, 2)
Frame_18.BackgroundTransparency = 0.7
Frame_18.Position = UDim2.new(0, 0, 0.2, 0)
Frame_18.Parent = Frame_4
Frame_18.ZIndex = 1
Frame_18.BorderSizePixel = 0
Frame_18.BackgroundColor3 = Color3.fromRGB(0, 80, 30)
local Frame_19 = Instance.new("Frame")
Frame_19.Size = UDim2.new(1, 0, 0, 2)
Frame_19.BackgroundTransparency = 0.7
Frame_19.Position = UDim2.new(0, 0, 0.3, 0)
Frame_19.Parent = Frame_4
Frame_19.ZIndex = 1
Frame_19.BorderSizePixel = 0
Frame_19.BackgroundColor3 = Color3.fromRGB(0, 80, 30)
local Frame_20 = Instance.new("Frame")
Frame_20.Size = UDim2.new(1, 0, 0, 2)
Frame_20.BackgroundTransparency = 0.7
Frame_20.Position = UDim2.new(0, 0, 0.4, 0)
Frame_20.Parent = Frame_4
Frame_20.ZIndex = 1
Frame_20.BorderSizePixel = 0
Frame_20.BackgroundColor3 = Color3.fromRGB(0, 80, 30)
local Frame_21 = Instance.new("Frame")
Frame_21.Size = UDim2.new(1, 0, 0, 2)
Frame_21.BackgroundTransparency = 0.7
Frame_21.Position = UDim2.new(0, 0, 0.5, 0)
Frame_21.Parent = Frame_4
Frame_21.ZIndex = 1
Frame_21.BorderSizePixel = 0
Frame_21.BackgroundColor3 = Color3.fromRGB(0, 80, 30)
local Frame_22 = Instance.new("Frame")
Frame_22.Size = UDim2.new(1, 0, 0, 2)
Frame_22.BackgroundTransparency = 0.7
Frame_22.Position = UDim2.new(0, 0, 0.6, 0)
Frame_22.Parent = Frame_4
Frame_22.ZIndex = 1
Frame_22.BorderSizePixel = 0
Frame_22.BackgroundColor3 = Color3.fromRGB(0, 80, 30)
local Frame_23 = Instance.new("Frame")
Frame_23.Size = UDim2.new(1, 0, 0, 2)
Frame_23.BackgroundTransparency = 0.7
Frame_23.Position = UDim2.new(0, 0, 0.7, 0)
Frame_23.Parent = Frame_4
Frame_23.ZIndex = 1
Frame_23.BorderSizePixel = 0
Frame_23.BackgroundColor3 = Color3.fromRGB(0, 80, 30)
local Frame_24 = Instance.new("Frame")
Frame_24.Size = UDim2.new(1, 0, 0, 2)
Frame_24.BackgroundTransparency = 0.7
Frame_24.Position = UDim2.new(0, 0, 0.8, 0)
Frame_24.Parent = Frame_4
Frame_24.ZIndex = 1
Frame_24.BorderSizePixel = 0
Frame_24.BackgroundColor3 = Color3.fromRGB(0, 80, 30)
local Frame_25 = Instance.new("Frame")
Frame_25.Size = UDim2.new(1, 0, 0, 2)
Frame_25.BackgroundTransparency = 0.7
Frame_25.Position = UDim2.new(0, 0, 0.9, 0)
Frame_25.Parent = Frame_4
Frame_25.ZIndex = 1
Frame_25.BorderSizePixel = 0
Frame_25.BackgroundColor3 = Color3.fromRGB(0, 80, 30)
local Frame_26 = Instance.new("Frame")
Frame_26.Size = UDim2.new(1, 0, 0, 2)
Frame_26.BackgroundTransparency = 0.7
Frame_26.Position = UDim2.new(0, 0, 1, 0)
Frame_26.Parent = Frame_4
Frame_26.ZIndex = 1
Frame_26.BorderSizePixel = 0
Frame_26.BackgroundColor3 = Color3.fromRGB(0, 80, 30)
local UICorner_3 = Instance.new("UICorner")
UICorner_3.CornerRadius = UDim.new(1, 0)
local UIStroke_2 = Instance.new("UIStroke")
UIStroke_2.Color = Color3.fromRGB(0, 255, 100)
UIStroke_2.Transparency = 0.5
UIStroke_2.Thickness = 2
local Frame_27 = Instance.new("Frame")
Frame_27.Size = UDim2.new(0, 3, 0, 3)
Frame_27.BackgroundTransparency = 0.46
Frame_27.Position = UDim2.new(0.42, 0, 0.71, 0)
Frame_27.Parent = Frame_3
Frame_27.ZIndex = 2
Frame_27.BorderSizePixel = 0
Frame_27.BackgroundColor3 = Color3.fromRGB(0, 255, 100)
UICorner_3.Parent = Frame_27
UIStroke_2.Parent = Frame_27
task.spawn(function()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.69
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.26
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.74
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.37
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.85
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.56
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.34
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.46
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.3
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.73
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.36
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.66
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.86
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.39
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.31
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.87
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.75
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.84
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.79
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.89
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.4
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.51
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.35
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.57
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.2
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.55
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.65
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.29
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.77
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.59
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(1.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.72
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.42
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.67
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.54
    })
    tween:Play()
    task.wait(1.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.61
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.32
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.8
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.44
    })
    tween:Play()
    task.wait(2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.58
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.68
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.47
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.6
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.48
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.27
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.63
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(2.5)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.88
    })
    tween:Play()
    task.wait(1.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.76
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.9
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.83
    })
    tween:Play()
    task.wait(2.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.71
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.62
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.41
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.25
    })
    tween:Play()
    task.wait(2.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.7
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.52
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.45
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.82
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.53
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.23
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.21
    })
    tween:Play()
    task.wait(1.9)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.24
    })
    tween:Play()
    task.wait(1.8)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.28
    })
    tween:Play()
    task.wait(2.2)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.33
    })
    tween:Play()
    task.wait(1.6)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.38
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.22
    })
    tween:Play()
    task.wait(3)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.43
    })
    tween:Play()
    task.wait(2.4)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.64
    })
    tween:Play()
    task.wait(1.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.49
    })
    tween:Play()
    task.wait(2.1)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.5
    })
    tween:Play()
    task.wait(1.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.78
    })
    tween:Play()
    task.wait(2.7)
    local tween = TweenService:Create(Frame_27, TweenInfo.new(1.5), {
    BackgroundTransparency = 0.81
    })
    tween:Play()
    task.wait(1.6)
end)
-- [PenguEnv] SECURITY: SECURITY: call rate exceeded
